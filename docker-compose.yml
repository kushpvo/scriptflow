services:
  scriptflow:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - scriptflow-data:/data
    environment:
      - DATA_DIR=/data
      - TZ=Europe/London
    restart: unless-stopped

volumes:
  scriptflow-data: